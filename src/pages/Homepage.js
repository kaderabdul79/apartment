import React from 'react';
import Slider from '../components/Slider/Slider';

const Homepage = () => {
    return (
        <div>
            <Slider></Slider>
        </div>
    );
};

export default Homepage;